[package]
name = "app_attest_android"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
base64 = "0.22"
josekit = "0.10"
# Again, to convert X.509 SPKI DER public key to P-256 coordinates
p256 = { version = "0.13", features = ["pkcs8"] }